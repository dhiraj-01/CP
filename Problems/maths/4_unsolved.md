### Problem
```
1) Given array, count total pairs (i, j) such that i < j and gcd(a[i], a[j]) == 1
2) Given array, count total pairs (i, j) such that i < j and gcd(a[i], a[j]) > 1
3) Given 2 arrays(a1, a2), count total pairs (i, j) such that gcd(a1[i], a2[j]) > 1
```

### Solution
```
idk :(
Hint: Mobius function, Inclusion Exclusion principle
```